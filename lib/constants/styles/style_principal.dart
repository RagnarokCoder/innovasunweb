import 'package:flutter/widgets.dart';

stylePrincipalBold(double size, Color color) {
  return TextStyle(color: color, fontSize: size, fontWeight: FontWeight.bold);
}

styleSecondary(double size, Color color) {
  return TextStyle(
    color: color,
    fontSize: size,
  );
}
